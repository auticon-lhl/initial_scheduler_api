const router = require("express").Router();

// import path

// import read function to enable you to read data from a file

module.exports = () => {
  router.get("/interviewers", (request, response) => {
    // Place code to get all interviewers below

  });

  return router;
};
